<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mediaplayerdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MediaPlayerDemo">
        <activity android:name=".Registartion"></activity>
        <activity android:name=".Navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Meditation_Screen" />
        <activity android:name=".Medtation_QA" />
        <activity android:name=".Dahsboard"></activity>
        <activity android:name=".SongList" />
        <activity android:name=".VideoPlayer" />
        <activity android:name=".AudioPlayer" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>